# TODOS

- [ ] Benchmark autoresearch loop throughput (experiments/hour) before and after backtest optimization. Single backtest timing doesn't capture the full picture — the loop includes strategy mutation, file I/O, and process startup overhead.
